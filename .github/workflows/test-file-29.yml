name: File 29 Test - Master Alignment And Coverage
on:
  workflow_dispatch:
jobs:
  test-file-29:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest
      - run: python -m pytest -q tests/test_master_alignment_coverage.py
        env:
          PYTHONPATH: .
